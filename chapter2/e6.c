/* Legibility of identifiers */
#include <stdio.h>

int main (void) {
  printf("current__balance has two unnecessary underscores,");
  printf(" with one underscore it is already legible enough therefore two underscores are unnecessary\n");

return 0;
}
