/* C Keywords */
#include <stdio.h>
int main (void) {
  printf("a(for) and e(while) are kewords in C\n");
  printf("c(main) and d(printf) are additional keywords\n");
  printf("b(If) is not a keyword because If starts with a capital letter\n");
  return 0;
}
